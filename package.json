{
  "name": "conjure-loans",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ richance/conjure-loans",
    "create-local": "graph create --node http://localhost:8020/ richance/conjure-loans",
    "remove-local": "graph remove --node http://localhost:8020/ richance/conjure-loans",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 richance/conjure-loans"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
